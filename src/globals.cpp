int num_procs;
int proc_id;
#include <string>
#include <set>
std::set<std::string> added_paths;
/*
#include <vector>
arbi_array<num1d> num_vect_to_arbi_array(vector<num> vect){
  arbi_array<num1d> ans; ans.resize(vect.size());
  for(int i = 0; i < vect.size(); i++){
    ans(i) = vect[i];
  }
  return ans;
}

vector<num> arbi_array_num_to_vect(arbi_array<num1d> ar){
  vector<num> ans(ar.size().i0);
  for(int i = 0; i < ans.size(); i++){
    ans[i] = ar(i);
  }
  return ans;
  }*/
